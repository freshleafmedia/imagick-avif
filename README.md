# PHP Imagick Extension With AVIF Support

This repo contains all the parts needed to build a Docker container which contains a copy of
the imagick PHP module with support for the AVIF image format. 
